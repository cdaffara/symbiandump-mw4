<?xml version="1.0" encoding="UTF-8"?>
<!--Sample XML file generated by XMLSPY v5 rel. 3 U (http://www.xmlspy.com)-->
<template-file xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.metrowerks.com/schemas/2003/IDE/SymbianLinkDescriptor.xsd">
	<template uuid="{AF9FEC09-7314-4940-9443-1644BD3648B5}">
		<version>1</version>
		<name>Symbian Link Descriptor</name>
		<aliases>
			<alias uuid="{AF9FEC10-7314-4940-9443-1644BD3648B5}">.</alias>
		</aliases>
		<setting uuid-alias="." entry="linkCommandSet">
			<array inheritance="none">
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>perl.exe -S prepdef.pl "${var:DEF_FILE}" "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.prep.def"</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>mwldsym2.exe "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.prep.def" -importlib -o "${var:KIT_EPOCROOT}\epoc32\release\winscw\udeb\${var:IMPORT_LIBRARY}" -addcommand "out:WebViewApiTest.dll" -warnings off</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>mwldsym2.exe -msgstyle gcc -stdlib ${var:FIRST_LIB}  -noentry -shared -subsystem windows ${var:LIBS} -o "${target.data}\${output.file.name}" -export dllexport -m __E32Dll -nocompactimportlib -implib "${target.data}\${var:IMPORT_LIBRARY}" -addcommand "out:${output.file.name}" -warnings off ${var:COMMON_LINK_FLAGS} -l "${target.data}\ObjectCode" -search ${var:LINK_OBJS_NO_PATH}</simple>
					</setting>
					<setting uuid-alias="." entry="firstLibProcessing">
						<simple>EDLL.LIB</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>cmd.exe /C del "${target.data}\${output.file.name}"</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>mwldsym2.exe -S -show only,names,unmangled,verbose -o "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.inf" "${target.data}\${var:IMPORT_LIBRARY}"</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>perl.exe -w -S makedef.pl  -absent __E32Dll -Inffile "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.inf" -Frzfile "\S60\mw\web\web_sdk\webview_api\tsrc\bwins\WebViewApiTestu.def" "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.def"</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>cmd.exe /C del "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.inf"</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>cmd.exe /C del "${target.data}\${var:IMPORT_LIBRARY}"</simple>
					</setting>
				</struct>
				<struct>
					<setting uuid-alias="." entry="linkCommand">
						<simple>mwldsym2.exe -msgstyle gcc -stdlib ${var:FIRST_LIB}  -noentry -shared -subsystem windows ${var:LIBS} -o "${output}\${output.file.name}" -map "${output}\${output.file.name}.map" -f "${target.data}\${var:IMPORT_LIBRARY_NO_EXT}.def" -noimplib  ${var:COMMON_LINK_FLAGS} -l "${target.data}\ObjectCode" -search ${var:LINK_OBJS_NO_PATH}</simple>
					</setting>
					<setting uuid-alias="." entry="firstLibProcessing">
						<simple>EDLL.LIB</simple>
					</setting>
				</struct>
			</array>
		</setting>
		<setting uuid-alias="." entry="symbolDefinitions">
			<array inheritance="none"/>
		</setting>
		<setting uuid-alias="." entry="textFileDumpDefinitions">
			<array inheritance="none">
			</array>
		</setting>
	</template>
</template-file>
